# Release and commit

The manuscript-associated version is GitHub release/tag
`paper-release-2026-07-r2`. The immutable commit hash is recorded in the release
metadata after the release commit is created. No Zenodo DOI is claimed.
